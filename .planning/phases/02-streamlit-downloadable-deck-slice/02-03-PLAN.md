---
phase: 02-streamlit-downloadable-deck-slice
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - .planning/phases/02-streamlit-downloadable-deck-slice/02-STREAMLIT-MANUAL-CHECK.md
autonomous: false
requirements:
  - PPT-01
  - PPT-04
  - VIS-01
  - VIS-02
user_setup: []
must_haves:
  truths:
    - "Automated tests pass for backend export, Streamlit helper state, and Streamlit PPT wiring."
    - "Manual Streamlit check confirms Create PPT transitions to Download PPT for a successful validated run."
    - "Manual Streamlit check confirms filename, MIME type, warnings, and ineligible-record behavior."
    - "Source scan confirms Streamlit calls only the backend exporter and does not render PPTX."
  artifacts:
    - path: ".planning/phases/02-streamlit-downloadable-deck-slice/02-STREAMLIT-MANUAL-CHECK.md"
      provides: "Manual Streamlit verification checklist and result notes"
      contains: "Create PPT"
  key_links:
    - from: "Streamlit UI"
      to: "src.agent.presentation_export.build_presentation_export"
      via: "manual Create PPT check"
      pattern: "Create PPT"
    - from: "Streamlit UI"
      to: "PresentationExport.filename"
      via: "manual Download PPT check"
      pattern: "Download PPT"
---

<objective>
Verify the completed Streamlit PPT export slice with automated checks and a manual UI checkpoint.

Purpose: Prove the Phase 2 user-facing flow and backend boundary after implementation.
Output: Manual verification checklist plus checkpoint result in the plan summary.
</objective>

## Phase Goal

**As a** Wuerth logistics analysis user, **I want to** download a Wuerth PowerPoint deck from a successful Streamlit analysis run, **so that** I can reuse the core narrative and evidence without manual deck assembly.

<execution_context>
@C:/Users/leonk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/leonk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-streamlit-downloadable-deck-slice/02-CONTEXT.md
@.planning/phases/02-streamlit-downloadable-deck-slice/02-UI-SPEC.md
@.planning/phases/02-streamlit-downloadable-deck-slice/02-RESEARCH.md
@.planning/phases/02-streamlit-downloadable-deck-slice/02-01-SUMMARY.md
@.planning/phases/02-streamlit-downloadable-deck-slice/02-02-SUMMARY.md
@streamlit_app.py
@src/agent/presentation_export.py
@evaluation/test_streamlit_presentation_export.py
@evaluation/test_presentation_export.py
</context>

<must_haves>
- PPT-01: Human check confirms successful Streamlit export to `.pptx`.
- PPT-04: Downloaded deck opens and contains the backend-generated narrative and evidence sections from Phase 1.
- VIS-01: Source scan confirms Streamlit did not add chart eligibility rules.
- VIS-02: A record with renderable bar or line chart evidence can create and download a deck through the UI.
- D-14 and D-15: Manual verification covers transition, filename, MIME type, warnings, and ineligible state, with a seeded or live successful record that satisfies backend eligibility.
- D-16: Automated source scan confirms Streamlit only calls the backend exporter.
</must_haves>

<source_audit>
| SOURCE | ID | Feature or Requirement | Plan | Status | Notes |
|--------|----|------------------------|------|--------|-------|
| GOAL | Phase 2 | Users can download a Wuerth deck from a successful Streamlit analysis run | 02-02, 02-03 | COVERED | Create PPT and Download PPT are implemented then manually verified. |
| REQ | PPT-01 | Export a successful validated analysis run as `.pptx` from Streamlit | 02-01, 02-02, 02-03 | COVERED | State helpers, UI wiring, and manual checkpoint cover it. |
| REQ | PPT-04 | Deck includes title or takeaway, summary, KPI, evidence, caveats, sources, and metadata | 02-02, 02-03 | COVERED | Streamlit passes the original record to the backend exporter and does not rebuild slide content. |
| REQ | VIS-01 | PowerPoint export reuses `visualization_spec.py` chart eligibility | 02-01, 02-02, 02-03 | COVERED | Tests and source scan block UI-side chart eligibility logic. |
| REQ | VIS-02 | Export simple bar and line chart evidence when renderable | 02-02, 02-03 | COVERED | Backend exporter receives the original record with chart plan or chart spec. |
| RESEARCH | R-01 | Use only existing dependencies and no new package install | 02-01, 02-02, 02-03 | COVERED | Plans add no package-manager install tasks. |
| RESEARCH | R-02 | Keep PPTX rendering behind `src.agent.presentation_export` | 02-01, 02-02, 02-03 | COVERED | Boundary tests and source scan enforce this. |
| RESEARCH | R-03 | Store generated bytes in Streamlit session state keyed by chat and run | 02-01, 02-02 | COVERED | Helpers and UI wiring implement the key shape. |
| RESEARCH | R-04 | Use backend filename and MIME type in download button | 02-02, 02-03 | COVERED | Wiring tests and manual check cover filename and MIME. |
| CONTEXT | D-01 to D-18 | Locked UI, eligibility, warning, option, and verification decisions | 02-01, 02-02, 02-03 | COVERED | Task actions cite the relevant decision IDs. Deferred ideas are excluded. |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Run automated phase verification and write manual checklist</name>
  <files>.planning/phases/02-streamlit-downloadable-deck-slice/02-STREAMLIT-MANUAL-CHECK.md</files>
  <read_first>
    @.planning/phases/02-streamlit-downloadable-deck-slice/02-UI-SPEC.md
    @.planning/phases/02-streamlit-downloadable-deck-slice/02-01-SUMMARY.md
    @.planning/phases/02-streamlit-downloadable-deck-slice/02-02-SUMMARY.md
    @streamlit_app.py
    @evaluation/test_streamlit_presentation_export.py
    @evaluation/test_presentation_export.py
  </read_first>
  <action>Run the automated verification commands below. Create `.planning/phases/02-streamlit-downloadable-deck-slice/02-STREAMLIT-MANUAL-CHECK.md` with exact manual steps for `streamlit run streamlit_app.py`, a successful validated analysis run, `Create PPT`, `Download PPT`, filename and MIME inspection, warnings display, and ineligible-record behavior. The checklist must state that a seeded or mock successful record is acceptable only if it satisfies `can_export_presentation(record).can_export` per D-15. The checklist must also state that no slide preview, slide controls, closing-slide control, or PPTX rendering logic belongs in Streamlit per D-09, D-10, D-11, and D-16.</action>
  <acceptance_criteria>
    - Backend export tests pass.
    - Streamlit PPT helper and wiring tests pass.
    - Syntax compilation passes for the app, source, scripts, and evaluation files.
    - Manual checklist file exists and covers transition, filename, MIME type, warnings, and ineligible state per D-14.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest evaluation.test_presentation_export evaluation.test_streamlit_presentation_export -v</automated>
    <automated>python -m compileall app src scripts streamlit_app.py evaluation</automated>
    <automated>powershell -NoProfile -Command "$source = Get-Content -Raw -LiteralPath 'streamlit_app.py'; if ($source -match 'from pptx import|from pptx\\.|Presentation\\(|SlideDeckSpec|SlideSpec|validate_template|template_path=|deck_spec') { exit 1 }; if ($source -notmatch 'build_presentation_export' -or $source -notmatch 'can_export_presentation' -or $source -notmatch 'PPTX_MIME_TYPE') { exit 1 }"</automated>
  </verify>
  <done>Automated tests and source boundary scans pass, and the manual checklist is ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Streamlit Create PPT to Download PPT flow</name>
  <files>.planning/phases/02-streamlit-downloadable-deck-slice/02-STREAMLIT-MANUAL-CHECK.md</files>
  <read_first>
    @.planning/phases/02-streamlit-downloadable-deck-slice/02-STREAMLIT-MANUAL-CHECK.md
    @.planning/phases/02-streamlit-downloadable-deck-slice/02-UI-SPEC.md
  </read_first>
  <action>Ask the user to run the manual checklist from `02-STREAMLIT-MANUAL-CHECK.md` or watch while the executor runs it locally. Verify the exact UI copy and behavior from D-14: `Create PPT`, `Creating PPT...`, `Download PPT`, `PPT ready.`, `PPT created with warnings.`, `PPT unavailable: {reason}`, and `PPT export failed: {reason}. Fix the template or rerun a valid analysis, then create the deck again.` Record approval or issues in the summary.</action>
  <what-built>Phase 2 Streamlit PPT export controls backed by `src.agent.presentation_export`.</what-built>
  <how-to-verify>
    1. Start Streamlit with `streamlit run streamlit_app.py`.
    2. Produce or seed a successful validated record where `can_export_presentation(record).can_export` is true.
    3. Confirm the result export row shows CSV, Excel, and `Create PPT`.
    4. Click `Create PPT` and confirm `Creating PPT...` appears during generation.
    5. Confirm `Download PPT` appears, uses the backend filename beginning with `wuerth_logistics_`, and downloads a `.pptx` with MIME type `application/vnd.openxmlformats-officedocument.presentationml.presentation`.
    6. Confirm backend warnings appear as `PPT created with warnings.` plus `PPT warnings` when warnings exist.
    7. Confirm an ineligible record shows disabled `Create PPT` plus `PPT unavailable: {reason}` and no active download.
  </how-to-verify>
  <acceptance_criteria>
    - Human verifier approves the visible Create PPT to Download PPT transition.
    - Human verifier confirms filename, MIME type, warning, and ineligible states.
    - Any issue found is recorded with exact observed behavior.
  </acceptance_criteria>
  <verify>
    <automated>Test-Path -LiteralPath '.planning/phases/02-streamlit-downloadable-deck-slice/02-STREAMLIT-MANUAL-CHECK.md'</automated>
    <human-check>Type `approved` after the manual Streamlit checklist passes, or describe the issue.</human-check>
  </verify>
  <resume-signal>Type `approved` or describe the failing step and observed behavior.</resume-signal>
  <done>Manual Streamlit verification is approved or documented with exact follow-up gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local Streamlit browser to app runtime | Manual verification exercises the browser-facing controls. |
| App runtime to backend exporter | Source scan proves the UI calls backend export functions without taking over rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | final source boundary | mitigate | Automated source scan blocks Streamlit-side PPTX renderer imports, template paths, and deck-spec inspection per D-16. |
| T-02-08 | Information Disclosure | manual download behavior | mitigate | Human check verifies the downloaded deck belongs to the active successful record and uses the backend filename per D-03 and D-12. |
| T-02-09 | Repudiation | manual verification evidence | mitigate | Record the manual checklist result and any observed issue in `02-03-SUMMARY.md` per D-14. |
| T-02-SC | Tampering | package installs | accept | Verification uses existing project dependencies and adds no package-manager install task. |
</threat_model>

<verification>
- `python -m unittest evaluation.test_presentation_export evaluation.test_streamlit_presentation_export -v`
- `python -m compileall app src scripts streamlit_app.py evaluation`
- PowerShell source scan for forbidden Streamlit PPTX renderer tokens
- Human Streamlit checklist approval
</verification>

<success_criteria>
- Automated tests pass for backend export and Streamlit PPT integration.
- Source scan proves PPTX rendering stays out of Streamlit.
- Human verifier confirms Create PPT, Download PPT, filename, MIME type, warnings, and ineligible state.
- Manual verification result is captured in `02-03-SUMMARY.md`.
</success_criteria>

<output>
Create `.planning/phases/02-streamlit-downloadable-deck-slice/02-03-SUMMARY.md` when done.
</output>
