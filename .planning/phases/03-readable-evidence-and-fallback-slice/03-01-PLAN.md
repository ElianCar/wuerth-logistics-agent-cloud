---
phase: 03-readable-evidence-and-fallback-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/presentation_planner.py
  - evaluation/test_presentation_export.py
autonomous: true
requirements: [PPT-05, VIS-03, VIS-04, TEST-04]
must_haves:
  truths:
    - "A successful result record can be profiled into a deterministic German presentation plan."
    - "Evidence table plans preserve SQL result order and carry visible row and column truncation notes."
    - "Unsupported chart requests produce a German fallback reason instead of blocking deck creation."
    - "Top-N categorical evidence plans cap labels and aggregate excess categories as Sonstige."
  artifacts:
    - path: "src/agent/presentation_planner.py"
      provides: "Pure presentation planning/profile layer"
      exports: ["build_presentation_plan", "derive_presentation_title", "format_management_number"]
    - path: "evaluation/test_presentation_export.py"
      provides: "Deterministic planner regression tests"
      contains: "PresentationPlannerContractTests"
  key_links:
    - from: "src/agent/presentation_planner.py"
      to: "src/agent/presentation_export.py"
      via: "build_presentation_plan(record=...) consumed by later export integration"
      pattern: "build_presentation_plan"
    - from: "evaluation/test_presentation_export.py"
      to: "src/agent/presentation_planner.py"
      via: "direct unittest imports"
      pattern: "from src\\.agent\\.presentation_planner import"
---

<objective>
Create the deterministic presentation planning/profile layer that Phase 3 builds on.

Purpose: The exporter needs a validated plan before rendering so titles, German executive copy, table pages, chart choices, fallback reasons, and warnings are decided before PowerPoint shapes are populated.
Output: A new pure backend module plus focused tests for German title derivation, number formatting, W05-style profiling, SQL-order table pages, unsupported chart fallback, and top-N categorical plans.
</objective>

<execution_context>
@C:/Users/leonk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/leonk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-readable-evidence-and-fallback-slice/03-CONTEXT.md
@.planning/phases/03-readable-evidence-and-fallback-slice/03-RESEARCH.md
@.planning/phases/03-readable-evidence-and-fallback-slice/03-PATTERNS.md
@.planning/phases/01-deterministic-backend-deck-slice/01-CONTEXT.md
@.planning/phases/02-streamlit-downloadable-deck-slice/02-CONTEXT.md
@src/agent/presentation_export.py
@src/agent/reporting_agent.py
@src/agent/visualization_spec.py
@evaluation/test_presentation_export.py
@requirements.txt
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | phase-03 | Readable evidence slides with overflow-safe fallbacks | 03-01, 03-02, 03-04 | COVERED | Planner defines budgets and evidence plans; renderer and regression plans prove output.
REQ | PPT-05 | Preserve SQL result order and mark row or column truncation | 03-01, 03-02, 03-04 | COVERED | Table page plans must keep original row position and visible German notes.
REQ | VIS-03 | Unsupported chart shapes fall back with a reason | 03-01, 03-02, 03-03, 03-04 | COVERED | Planner creates fallback metadata; exporter renders it; optional JSON path must fallback locally.
REQ | VIS-04 | Top-N categorical comparisons stay readable | 03-01, 03-02, 03-04 | COVERED | Top-N chart plans cap labels and aggregate Sonstige.
REQ | TEST-04 | Long text, empty result, unsupported chart shape, and truncation tests | 03-01, 03-02, 03-04 | COVERED | Unit and PPTX reopen tests cover all four cases.
RESEARCH | deterministic-plan | Add a pure PresentationPlan layer before SlideDeckSpec | 03-01 | COVERED | New module owns profiling and plan validation.
RESEARCH | german-narrative | German visible text, short titles, number formatting | 03-01, 03-02 | COVERED | Planner owns title, bullets, metrics; exporter renders them.
RESEARCH | rich-text | Bold key executive summary facts with PowerPoint runs | 03-02 | COVERED | Renderer integration plan owns run-level formatting.
RESEARCH | top-n | Horizontal top-N bars with label caps and Sonstige | 03-01, 03-02 | COVERED | Planner creates the chart payload; exporter renders it.
RESEARCH | optional-json | Optional LLM JSON planning only through local validation and fallback | 03-03 | COVERED | Disabled default, fake-tested, same plan schema.
RESEARCH | test-runtime | Use .venv\Scripts\python.exe for commands | all | COVERED | Every verify command uses .\.venv\Scripts\python.exe.
CONTEXT | generation-strategy | Keep python-pptx renderer and backend boundary | 03-01, 03-02 | COVERED | Planner is pure backend and does not render PPTX.
CONTEXT | analytical-content | W05 no-invoice outputs should expose order, duplicate, customer material, and ship-to-party evidence | 03-01, 03-02, 03-04 | COVERED | Planner profiles these columns when present.
CONTEXT | layout-overflow | Long text and labels must be capped before rendering | 03-01, 03-02 | COVERED | Planner emits budgeted strings and warnings.
CONTEXT | streamlit-boundary | Streamlit remains a thin caller and warning surface | 03-04 | COVERED | Boundary tests remain in Streamlit plan.
CONTEXT | deferred-items | Memory/RBAC, final docs, slide designer, direct Claude PPTX | none | EXCLUDED | Explicitly deferred or disallowed by Phase 3 context.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define deterministic planner contracts and formatting rules</name>
  <files>src/agent/presentation_planner.py, evaluation/test_presentation_export.py</files>
  <read_first>
    @.planning/phases/03-readable-evidence-and-fallback-slice/03-CONTEXT.md
    @.planning/phases/03-readable-evidence-and-fallback-slice/03-PATTERNS.md
    @src/agent/reporting_agent.py
    @src/agent/presentation_export.py
    @evaluation/test_presentation_export.py
  </read_first>
  <behavior>
    - `derive_presentation_title(record)` returns a German title at or below 52 characters, does not equal the raw `user_question`, and falls back to `Logistik-Auswertung`.
    - `format_management_number(value, percentage=False)` formats integers with German separators, large values as `Mio.` or `Mrd.`, and percentages with at most one decimal.
    - New frozen dataclasses are JSON-serializable through explicit `to_dict()` helpers or simple primitive fields.
    - Importing `src.agent.presentation_planner` does not import Streamlit, pptx, database adapters, or live model clients.
  </behavior>
  <action>Add `PresentationPlan`, `ExecutiveBullet`, `TextSpan`, `EvidenceChartPlan`, `EvidenceTablePage`, `PlanningAudit`, `derive_presentation_title`, and `format_management_number` in `src/agent/presentation_planner.py`. First add `PresentationPlannerContractTests` in `evaluation/test_presentation_export.py` for the behaviors above, then implement the helpers. Keep the module pure: it may inspect dictionaries, lists, strings, decimals, and `reporting_result`, but it must not call SQL, Streamlit, PowerPoint rendering, or LLM functions.</action>
  <acceptance_criteria>
    - Planner title tests prove long raw questions are shortened into German analysis titles and the raw question is absent from the cover-title value.
    - Number formatting tests cover whole counts, decimal values, percentages, millions, and billions without false precision.
    - A subprocess import test confirms `streamlit`, `streamlit_app`, and `pptx` are absent from `sys.modules` after importing the planner module.
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m unittest evaluation.test_presentation_export.PresentationPlannerContractTests -v</automated>
    <automated>.\.venv\Scripts\python.exe -m compileall src/agent/presentation_planner.py evaluation/test_presentation_export.py</automated>
  </verify>
  <done>`PresentationPlannerContractTests` pass and `src/agent/presentation_planner.py` exposes the deterministic contract helpers without UI, PPTX, database, or live-model imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build SQL-order table pages, W05 profiles, chart decisions, and fallback plans</name>
  <files>src/agent/presentation_planner.py, evaluation/test_presentation_export.py</files>
  <read_first>
    @.planning/REQUIREMENTS.md
    @.planning/phases/03-readable-evidence-and-fallback-slice/03-RESEARCH.md
    @src/agent/visualization_spec.py
    @src/agent/presentation_export.py
    @evaluation/test_presentation_export.py
  </read_first>
  <behavior>
    - `build_presentation_plan(record=...)` preserves SQL row order in `EvidenceTablePage.rows` and records original 1-based row ranges like `Zeilen 1-10 von 50`.
    - Wide results cap table columns at the existing export budget and include `Weitere Spalten ausgeblendet` in the visible note data.
    - W05-like records with `order_number`, `shiptoparty`, `customer_material`, and `shipment_rows` produce executive bullets for distinct orders, returned rows, total unmatched shipment rows, duplicate order evidence, top customer material, and top ship-to-party groups when present.
    - Top-N categorical charts use `chart_type="top_n_bar"`, `orientation="horizontal"`, deterministic descending count order with label tie-breakers, label caps, and `Sonstige` for excess categories.
    - Unsupported or empty chart shapes produce a German fallback reason and a table or limitation plan instead of raising.
  </behavior>
  <action>Extend `PresentationPlannerEvidenceTests` in `evaluation/test_presentation_export.py`, then implement `build_presentation_plan(record=...)`, result column profiling, W05-specific aggregate extraction, `EvidenceTablePage` slicing, `EvidenceChartPlan` top-N categorical generation, fallback reason generation, warning codes, and planning audit fields. The audit must record `planning_mode="deterministic"`, selected chart types, row and column truncation flags, and fallback reasons. Do not modify `streamlit_app.py` in this plan.</action>
  <acceptance_criteria>
    - SQL-order table tests compare the first and last visible row values against the input order.
    - Top-N tests prove categories over budget are aggregated into exactly one `Sonstige` row and labels stay within the planner label budget.
    - Unsupported chart tests prove a German fallback reason is present and deck creation remains eligible for the renderer integration plan.
    - Empty result tests prove planner behavior is deterministic while export eligibility remains owned by `can_export_presentation`.
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m unittest evaluation.test_presentation_export.PresentationPlannerEvidenceTests -v</automated>
    <automated>.\.venv\Scripts\python.exe -m compileall src/agent/presentation_planner.py evaluation/test_presentation_export.py</automated>
  </verify>
  <done>`build_presentation_plan()` returns a complete deterministic plan with German title, executive bullets, evidence table pages, top-N chart plans, fallback metadata, warnings, and audit data for successful records.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator record to planner | User question, SQL result cells, and reporting text enter deterministic planning from a prior successful run. |
| planner to later renderer | Budgeted title, body text, chart data, table slices, warnings, and audit metadata become render inputs in later plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | `build_presentation_plan` | mitigate | Normalize row and column values, avoid executing record content, and preserve source row order by index rather than by user text. |
| T-03-01-02 | Information Disclosure | `presentation_planner.py` | mitigate | Keep deterministic planning local and do not import or call live model clients in this plan. |
| T-03-01-03 | Denial of Service | table and top-N planning | mitigate | Cap rows, columns, label length, body text, and category count before plan data reaches the renderer. |
| T-03-01-04 | Repudiation | planning audit | mitigate | Record planning mode, chart choices, fallback reasons, and truncation flags in `PlanningAudit`. |
| T-03-SC | Tampering | package installs | accept | No package-manager install is planned; existing `python-pptx`, `matplotlib`, and `pandas` remain as audited in research. |
</threat_model>

<verification>
Run targeted planner tests and compile checks:
- `.\.venv\Scripts\python.exe -m unittest evaluation.test_presentation_export.PresentationPlannerContractTests evaluation.test_presentation_export.PresentationPlannerEvidenceTests -v`
- `.\.venv\Scripts\python.exe -m compileall src/agent/presentation_planner.py evaluation/test_presentation_export.py`
</verification>

<success_criteria>
- `src/agent/presentation_planner.py` exists and is importable without Streamlit, pptx, database adapters, or live model clients.
- Planner output covers title, language, executive bullets, emphasis spans, charts, table pages, caveats, warnings, and audit metadata.
- Planner tests prove PPT-05, VIS-03, VIS-04, and the planner portion of TEST-04.
</success_criteria>

<output>
Create `.planning/phases/03-readable-evidence-and-fallback-slice/03-01-SUMMARY.md` when done.
</output>
