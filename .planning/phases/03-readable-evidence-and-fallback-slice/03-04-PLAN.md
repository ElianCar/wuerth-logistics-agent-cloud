---
phase: 03-readable-evidence-and-fallback-slice
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - src/agent/presentation_planner.py
  - src/agent/presentation_export.py
  - streamlit_app.py
  - evaluation/test_presentation_export.py
  - evaluation/test_streamlit_presentation_export.py
autonomous: true
requirements: [PPT-05, VIS-03, VIS-04, TEST-04]
must_haves:
  truths:
    - "Long text, empty result, unsupported chart shape, and table truncation regressions are covered by automated tests."
    - "A W05-style result generates a reopened PPTX with German title, bold executive text, analytical top-N evidence, fallback handling, and visible truncation notes."
    - "Streamlit remains a thin create/download caller and surfaces concise backend warnings without slide-builder controls."
    - "The full Phase 3 test set runs through the repo virtual environment."
  artifacts:
    - path: "evaluation/test_presentation_export.py"
      provides: "End-to-end Phase 3 PPTX regression tests"
      contains: "PresentationExportRegressionTests"
    - path: "evaluation/test_streamlit_presentation_export.py"
      provides: "Streamlit warning and boundary regression tests"
      contains: "StreamlitPresentationExportWarningTests"
    - path: "streamlit_app.py"
      provides: "Thin warning display for backend export warnings"
      exports: ["render_presentation_export_feedback"]
  key_links:
    - from: "streamlit_app.py"
      to: "src.agent.presentation_export"
      via: "allowed imports only: PPTX_MIME_TYPE, build_presentation_export, can_export_presentation"
      pattern: "from src\\.agent\\.presentation_export import"
    - from: "evaluation/test_presentation_export.py"
      to: "src/agent/presentation_export.py"
      via: "PPTX reopen tests using python-pptx"
      pattern: "Presentation\\(BytesIO\\("
---

<objective>
Close Phase 3 with regression coverage and a thin Streamlit warning surface.

Purpose: The readable-evidence behavior must stay protected after implementation, and Streamlit must surface backend warnings without taking ownership of planning, chart selection, or PPT rendering.
Output: Automated regression tests for TEST-04, optional hardening if those tests expose gaps, and Streamlit warning copy that keeps the Phase 2 boundary intact.
</objective>

<execution_context>
@C:/Users/leonk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/leonk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-readable-evidence-and-fallback-slice/03-CONTEXT.md
@.planning/phases/03-readable-evidence-and-fallback-slice/03-RESEARCH.md
@.planning/phases/03-readable-evidence-and-fallback-slice/03-PATTERNS.md
@.planning/phases/01-deterministic-backend-deck-slice/01-CONTEXT.md
@.planning/phases/02-streamlit-downloadable-deck-slice/02-CONTEXT.md
@.planning/phases/03-readable-evidence-and-fallback-slice/03-03-SUMMARY.md
@src/agent/presentation_planner.py
@src/agent/presentation_export.py
@streamlit_app.py
@evaluation/test_presentation_export.py
@evaluation/test_streamlit_presentation_export.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add full Phase 3 PPTX regression tests and harden gaps they expose</name>
  <files>src/agent/presentation_planner.py, src/agent/presentation_export.py, evaluation/test_presentation_export.py</files>
  <read_first>
    @.planning/REQUIREMENTS.md
    @.planning/phases/03-readable-evidence-and-fallback-slice/03-CONTEXT.md
    @.planning/phases/03-readable-evidence-and-fallback-slice/03-PATTERNS.md
    @src/agent/presentation_planner.py
    @src/agent/presentation_export.py
    @evaluation/test_presentation_export.py
  </read_first>
  <behavior>
    - `PresentationExportRegressionTests` covers a W05-style record with `order_number`, `shiptoparty`, `customer_material`, `shipment_rows`, long user question, long text fields, more than 10 rows, and more than 5 columns.
    - Reopened PPTX text includes German management labels, a short cover title, `Zeilen 1-10 von`, `Weitere Spalten ausgeblendet`, a chart or fallback reason, and `Sonstige` when top-N aggregation applies.
    - Reopened PPTX run inspection finds at least one bold executive-summary run.
    - Empty result behavior is tested through `build_presentation_export()` and keeps the existing unavailable reason instead of rendering an invalid deck.
    - Unsupported chart shape behavior is tested on an otherwise eligible record and returns an available deck with a visible German fallback reason.
  </behavior>
  <action>Add `PresentationExportRegressionTests` to `evaluation/test_presentation_export.py` and run it against the implemented planner/exporter. If tests reveal gaps, adjust only `src/agent/presentation_planner.py` or `src/agent/presentation_export.py` to make the tested Phase 3 behavior true. Preserve existing Phase 1 and Phase 2 eligibility, template safety, deterministic byte normalization, and dynamic slide ordering behaviors.</action>
  <acceptance_criteria>
    - TEST-04 cases are all automated: long text, empty result, unsupported chart shape, and table truncation.
    - W05-style deck tests prove analytical evidence is more than a repeated raw table by checking planned top-N or fallback evidence text.
    - Full `evaluation.test_presentation_export` passes, not only the new regression class.
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m unittest evaluation.test_presentation_export.PresentationExportRegressionTests -v</automated>
    <automated>.\.venv\Scripts\python.exe -m unittest evaluation.test_presentation_export -v</automated>
    <automated>.\.venv\Scripts\python.exe -m compileall src/agent/presentation_planner.py src/agent/presentation_export.py evaluation/test_presentation_export.py</automated>
  </verify>
  <done>Phase 3 PPTX behavior is protected by reopened-deck tests covering readable evidence, fallback handling, bold executive runs, truncation notes, and empty-result availability behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Localize backend warning display while preserving the Streamlit boundary</name>
  <files>streamlit_app.py, evaluation/test_streamlit_presentation_export.py</files>
  <read_first>
    @.planning/phases/02-streamlit-downloadable-deck-slice/02-CONTEXT.md
    @.planning/phases/03-readable-evidence-and-fallback-slice/03-CONTEXT.md
    @src/agent/presentation_export.py
    @streamlit_app.py
    @evaluation/test_streamlit_presentation_export.py
  </read_first>
  <behavior>
    - Streamlit imports only `PPTX_MIME_TYPE`, `build_presentation_export`, and `can_export_presentation` from `src.agent.presentation_export`.
    - `render_presentation_export_feedback()` surfaces backend warnings in concise user-facing copy without importing planner dataclasses, slide specs, python-pptx, matplotlib, or template validators.
    - Known warning codes or prefixes for planner fallback, table truncation, chart fallback, and label truncation map to short German or existing app-language copy.
    - Download button behavior, stored exports, Create PPT action, MIME usage, and failure behavior from Phase 2 remain unchanged.
  </behavior>
  <action>Add `StreamlitPresentationExportWarningTests` to `evaluation/test_streamlit_presentation_export.py`, then update `streamlit_app.py` with a small `format_presentation_warning(warning)` helper used inside `render_presentation_export_feedback()`. The helper may map backend warning codes such as `presentation_planner_fallback`, `presentation_table_truncated`, `presentation_chart_fallback`, and `presentation_label_truncated`, but it must treat unknown warnings as backend text and must not inspect `deck_spec`, slide types, chart payloads, table rows, or template data.</action>
  <acceptance_criteria>
    - Streamlit warning tests prove mapped warnings appear in the expander and unknown backend warnings still appear.
    - Existing Streamlit create/download tests still pass.
    - Source-boundary tests continue to block renderer internals and slide-builder controls.
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m unittest evaluation.test_streamlit_presentation_export.StreamlitPresentationExportWarningTests -v</automated>
    <automated>.\.venv\Scripts\python.exe -m unittest evaluation.test_streamlit_presentation_export -v</automated>
    <automated>.\.venv\Scripts\python.exe -m compileall streamlit_app.py evaluation/test_streamlit_presentation_export.py</automated>
  </verify>
  <done>Streamlit remains a thin backend-export caller and displays Phase 3 backend warnings without adding PPT planning, rendering, or slide-control logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend export to Streamlit | PPTX bytes, filename, MIME type, warnings, and unavailable reason enter UI display code. |
| regression fixtures to exporter | Synthetic W05-style records exercise planner and renderer behavior without external systems. |
| Streamlit session state | Generated export objects persist per chat/run key for download. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | Streamlit warning display | mitigate | Treat warnings as text only, map known codes through fixed copy, and never execute or parse slide payloads in Streamlit. |
| T-03-04-02 | Information Disclosure | warning copy | mitigate | Display concise backend warnings and avoid exposing raw unbounded rows, secrets, or template package internals. |
| T-03-04-03 | Denial of Service | regression deck generation | mitigate | Use small synthetic records and existing row/column/chart caps in tests. |
| T-03-04-04 | Elevation of Privilege | Streamlit export controls | accept | Phase 3 does not add app authentication or memory RBAC; it preserves the existing backend eligibility gate. |
| T-03-SC | Tampering | package installs | accept | No package-manager install is planned; all regression checks use existing dependencies. |
</threat_model>

<verification>
Run the full Phase 3 regression set:
- `.\.venv\Scripts\python.exe -m unittest evaluation.test_presentation_export evaluation.test_streamlit_presentation_export -v`
- `.\.venv\Scripts\python.exe -m compileall app src scripts streamlit_app.py evaluation`
- PowerShell source scan: `$source = Get-Content -Raw streamlit_app.py; if ($source -cmatch 'from pptx import|Presentation\(|SlideDeckSpec|SlideSpec|validate_template|validate_slide_deck_spec|build_slide_deck_spec|_render_presentation|deck_spec|include_closing=True|slide_order|slide preview|chart type') { exit 1 }`
</verification>

<success_criteria>
- TEST-04 is covered by automated tests.
- Full presentation export and Streamlit export test modules pass in the repo virtual environment.
- Streamlit warning display stays thin and does not import or inspect renderer internals.
- The implementation remains PPT-only and does not add memory/RBAC or final documentation scope.
</success_criteria>

<output>
Create `.planning/phases/03-readable-evidence-and-fallback-slice/03-04-SUMMARY.md` when done.
</output>
