---
phase: 01-deterministic-backend-deck-slice
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/agent/presentation_export.py
  - evaluation/test_presentation_export.py
autonomous: true
requirements:
  - PPT-02
  - PPT-03
  - PPT-06
  - SPEC-01
  - SPEC-02
  - SPEC-03
  - SPEC-04
  - TEST-01
  - TEST-02
  - TEST-03
must_haves:
  truths:
    - "D-03: Template path resolution and manifest validation are explicit and test-covered."
    - "D-04: Known think-cell OLE embeddings warn only, while macros and external relationships block export."
    - "D-05: Invalid export handling remains backend-only and leaves `streamlit_app.py` untouched."
    - "D-06: Ineligible records never produce PPTX bytes through an alternate model-generated path."
  artifacts:
    - path: "src/agent/presentation_export.py"
      provides: "Eligibility gate, missing-template handling, template safety scan, and spec validation hardening"
      exports:
        - "can_export_presentation"
        - "validate_template"
        - "validate_slide_deck_spec"
    - path: "evaluation/test_presentation_export.py"
      provides: "Eligibility, missing-template, and template safety tests"
  key_links:
    - from: "src/agent/presentation_export.py"
      to: "assets/templates/PPT_Vorlage_Wuerth.pptx"
      via: "repo-relative default path"
      pattern: "DEFAULT_TEMPLATE_PATH"
    - from: "evaluation/test_presentation_export.py"
      to: "src/agent/presentation_export.py"
      via: "ineligible record and template package tests"
      pattern: "validate_template"
---

<objective>
Harden the backend export slice so invalid records fail clearly before rendering and unsafe template findings block export while known think-cell OLE objects remain warnings.

Purpose: Phase 1 is only complete if successful records export and invalid records cannot accidentally produce a deck.
Output: Test-covered eligibility, missing-template handling, spec validation, and PPTX template safety scanning.
</objective>

## Phase Goal

**As a** Wuerth logistics analysis user, **I want to** turn a successful validated orchestrator record into deterministic Wuerth PPTX bytes, **so that** I can get a presentation-ready backend deck output without manual file creation.

<execution_context>
@C:/Users/leonk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/leonk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-deterministic-backend-deck-slice/01-CONTEXT.md
@.planning/phases/01-deterministic-backend-deck-slice/01-RESEARCH.md
@.planning/phases/01-deterministic-backend-deck-slice/01-PATTERNS.md
@.planning/PPT_TEMPLATE_GUIDE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/TESTING.md
@.planning/phases/01-deterministic-backend-deck-slice/01-02-SUMMARY.md
@src/agent/presentation_export.py
@evaluation/test_presentation_export.py
</context>

<decision_trace>
D-01: Dynamic slide creation must not produce a fixed always-9-slide deck, and Agent 04/05 evidence layouts may repeat.
D-02: Agent 09 Closing is optional and must not be hardcoded into every deck.
D-03: The renderer targets the repo-relative Wuerth template and uses a manifest for layout names, aliases, placeholder indexes, and known warnings.
D-04: Remaining think-cell OLE embeddings are warnings when macros and external relationships are absent; macros and external relationships block export.
D-05: Do not modify `streamlit_app.py` in Phase 1.
D-06: Do not add direct Claude/Opus PPTX generation or runtime LLM-generated slide code.
D-07: Use deterministic local rendering, likely `python-pptx`, behind a package verification gate.
</decision_trace>

<runtime_preflight>
Automated verification commands are PowerShell commands. Each command resolves `$PythonExe` to `C:\Users\leonk\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe` when present, otherwise falls back to `docker-compose run --rm app /usr/local/bin/python3` only when the bundled runtime is absent.
</runtime_preflight>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing eligibility and missing-template tests</name>
  <files>evaluation/test_presentation_export.py</files>
  <read_first>
    - `.planning/REQUIREMENTS.md`
    - `.planning/phases/01-deterministic-backend-deck-slice/01-CONTEXT.md`
    - `.planning/phases/01-deterministic-backend-deck-slice/01-PATTERNS.md`
    - `.planning/codebase/TESTING.md`
    - `src/agent/presentation_export.py`
    - `evaluation/test_presentation_export.py`
  </read_first>
  <behavior>
    - Failed SQL execution returns unavailable export with empty bytes and a reason mentioning execution.
    - Failed validation or `sql_valid=False` returns unavailable export with empty bytes and a reason mentioning validation.
    - Clarification-only and `blocked_or_unsafe=True` records return unavailable export before template loading.
    - Missing columns, missing rows, or zero row count returns unavailable export with empty bytes.
    - A valid record with a missing or unreadable template path returns a clear template error without raising.
    - An ineligible record with a missing template path still reports the record eligibility reason first.
  </behavior>
  <action>Extend `evaluation/test_presentation_export.py` with `PresentationExportEligibilityTests` using table-driven `self.subTest` cases over valid-record overrides. Follow the `01-PATTERNS.md` analogs from `evaluation/test_orchestrator.py` for fake orchestrator records and stopped-before-work assertions, `evaluation/test_reporting_agent.py` for inline helper factories, and `evaluation/test_visualization_spec.py` for fail-closed assertions. Cover `execution_success=False`, `validation_success=False`, `sql_valid=False`, `needs_clarification=True`, `blocked_or_unsafe=True`, missing `query_result["columns"]`, empty `query_result["rows"]`, and `row_count=0` per PPT-03 and TEST-03. Add a `tempfile.TemporaryDirectory()` missing-template case for PPT-06 and TEST-01. Assert every unavailable result has `available is False`, `content == b""`, a non-empty `unavailable_reason`, and no uncaught exception. Do not modify or import `streamlit_app.py` per D-05.</action>
  <verify>
    <automated>$PythonExe = "C:\Users\leonk\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe"; if (Test-Path -LiteralPath $PythonExe) { cmd /c $PythonExe -m unittest evaluation.test_presentation_export.PresentationExportEligibilityTests } elseif (Get-Command docker-compose -ErrorAction SilentlyContinue) { docker-compose run --rm app /usr/local/bin/python3 -m unittest evaluation.test_presentation_export.PresentationExportEligibilityTests } else { throw "No bundled Python runtime or docker-compose fallback available." }</automated>
  </verify>
  <acceptance_criteria>
    - Every PPT-03 blocked condition has a named test case.
    - Missing template behavior is user-facing through structured export metadata and does not crash.
    - Ineligible records fail before template validation.
    - Tests are deterministic and do not call LLMs, databases, Streamlit, or Microsoft PowerPoint.
  </acceptance_criteria>
  <done>Eligibility and missing-template tests fail against the current implementation until the backend gate is hardened.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add failing template safety and spec validation tests</name>
  <files>evaluation/test_presentation_export.py</files>
  <read_first>
    - `.planning/PPT_TEMPLATE_GUIDE.md`
    - `.planning/phases/01-deterministic-backend-deck-slice/01-CONTEXT.md`
    - `.planning/phases/01-deterministic-backend-deck-slice/01-RESEARCH.md`
    - `src/agent/presentation_export.py`
    - `evaluation/test_presentation_export.py`
    - `assets/templates/PPT_Vorlage_Wuerth.pptx`
  </read_first>
  <behavior>
    - `validate_template(DEFAULT_TEMPLATE_PATH)` succeeds for the tracked Wuerth template and reports known OLE or think-cell warnings.
    - A copied PPTX containing `ppt/vbaProject.bin` is blocked as a macro safety finding.
    - A copied PPTX containing a `.rels` entry with `TargetMode="External"` is blocked as an external relationship safety finding.
    - `validate_slide_deck_spec` rejects unsupported slide types, missing required content, text budget overflow, table limit overflow, and unsupported chart payloads before `Presentation(...)` is called.
  </behavior>
  <action>Extend `evaluation/test_presentation_export.py` with `PresentationExportTemplateSafetyTests`. Follow the `01-PATTERNS.md` analogs from `evaluation/test_orchestrator.py` for `tempfile.TemporaryDirectory()` filesystem isolation and from `evaluation/test_visualization_spec.py` for direct invalid-spec assertions. Use `shutil.copyfile`, `tempfile.TemporaryDirectory()`, and `zipfile.ZipFile` to create temporary PPTX variants from `assets/templates/PPT_Vorlage_Wuerth.pptx`. Add `ppt/vbaProject.bin` to simulate a macro-bearing template. Add or modify a relationship file containing `TargetMode="External"` to simulate linked external content. Assert known OLE entries in the real template are warnings only per D-04, while macros and external relationships make the template audit invalid. Add spec validation tests that construct invalid `SlideDeckSpec` or `SlideSpec` objects directly and assert validation errors occur before rendering per SPEC-01 and SPEC-02.</action>
  <verify>
    <automated>$PythonExe = "C:\Users\leonk\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe"; if (Test-Path -LiteralPath $PythonExe) { cmd /c $PythonExe -m unittest evaluation.test_presentation_export.PresentationExportTemplateSafetyTests } elseif (Get-Command docker-compose -ErrorAction SilentlyContinue) { docker-compose run --rm app /usr/local/bin/python3 -m unittest evaluation.test_presentation_export.PresentationExportTemplateSafetyTests } else { throw "No bundled Python runtime or docker-compose fallback available." }</automated>
  </verify>
  <acceptance_criteria>
    - The real tracked template validates with warnings for existing OLE entries but no blocking findings.
    - Macro and external relationship simulations are blocking findings.
    - Unsupported slide specs are rejected without opening or saving a PPTX file.
    - Tests use only temporary copies and never modify the tracked template asset.
  </acceptance_criteria>
  <done>Template safety and spec validation tests fail until the implementation blocks unsafe templates and invalid specs correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement eligibility and template safety hardening</name>
  <files>src/agent/presentation_export.py, evaluation/test_presentation_export.py</files>
  <read_first>
    - `.planning/phases/01-deterministic-backend-deck-slice/01-CONTEXT.md`
    - `.planning/phases/01-deterministic-backend-deck-slice/01-RESEARCH.md`
    - `.planning/PPT_TEMPLATE_GUIDE.md`
    - `.planning/codebase/ARCHITECTURE.md`
    - `.planning/codebase/TESTING.md`
    - `src/agent/presentation_export.py`
    - `evaluation/test_presentation_export.py`
  </read_first>
  <behavior>
    - Export order is eligibility gate, input normalization, dynamic slide spec creation, spec validation, template validation, rendering.
    - Expected invalid records return structured unavailable exports instead of exceptions.
    - Missing or unreadable template returns a structured unavailable export that a future Streamlit caller can display.
    - Template safety scan warns for known OLE and blocks macros or external relationships.
  </behavior>
  <action>Harden `src/agent/presentation_export.py` by adding `can_export_presentation(record)` and using it as the first step inside `build_presentation_export`. Follow the `01-PATTERNS.md` analogs from `src/agent/visualization_spec.py` for fail-closed ordering and complete unavailable results, and from `src/agent/reporting_agent.py` for deterministic backend output boundaries and structured warnings. Reject `blocked_or_unsafe`, `needs_clarification`, failed `execution_success`, failed `validation_success`, `sql_valid=False`, missing columns, missing rows, and zero row count before template access per PPT-03. Return a complete `PresentationExport` with `available=False`, `content=b""`, `unavailable_reason`, `warnings`, `template_audit`, and no thrown exception for expected invalid input. Implement `validate_template` with `zipfile` scanning for missing path, unreadable package, required layout names from `TemplateManifest`, `vbaProject.bin`, external relationship targets, embedded object warnings, and known think-cell OLE handling per D-03 and D-04. Warn on template hash mismatch when required layouts exist and no macros or external relationships are present; block missing required layouts, macros, external relationships, unexpected active content, or unreadable template. Implement `validate_slide_deck_spec` so unsupported slide types, required-field gaps, text budgets, table limits, chart limits, table cap overflow, and unsupported renderer inputs are rejected before rendering per SPEC-01 and SPEC-02. Use a small deterministic Phase 1 table cap suitable for tests and basic evidence, and record truncation warnings while leaving richer top-N/readability tuning to Phase 3. Keep `streamlit_app.py` unchanged and do not add any direct model-generated PPTX or runtime code-generation path per D-05 and D-06.</action>
  <verify>
    <automated>$PythonExe = "C:\Users\leonk\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe"; if (Test-Path -LiteralPath $PythonExe) { cmd /c $PythonExe -m unittest evaluation.test_presentation_export } elseif (Get-Command docker-compose -ErrorAction SilentlyContinue) { docker-compose run --rm app /usr/local/bin/python3 -m unittest evaluation.test_presentation_export } else { throw "No bundled Python runtime or docker-compose fallback available." }</automated>
    <automated>$PythonExe = "C:\Users\leonk\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe"; if (Test-Path -LiteralPath $PythonExe) { cmd /c $PythonExe -m compileall src/agent/presentation_export.py evaluation/test_presentation_export.py } elseif (Get-Command docker-compose -ErrorAction SilentlyContinue) { docker-compose run --rm app /usr/local/bin/python3 -m compileall src/agent/presentation_export.py evaluation/test_presentation_export.py } else { throw "No bundled Python runtime or docker-compose fallback available." }</automated>
  </verify>
  <acceptance_criteria>
    - All tests in `evaluation/test_presentation_export.py` pass.
    - Missing template and invalid-record outputs are structured and contain no PPTX bytes.
    - Existing think-cell OLE entries are warnings only, while macros and external relationships block export.
    - `streamlit_app.py` remains unmodified in Phase 1.
  </acceptance_criteria>
  <done>The backend exporter is fail-closed for invalid records and unsafe templates while preserving the successful dynamic PPTX slice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Orchestrator record to eligibility gate | User-driven analysis output enters the export module as a mutable dictionary. |
| PPTX template package to renderer | The renderer opens a ZIP/OOXML package that could contain active content or external links. |
| Slide spec to renderer | The deterministic renderer consumes structured slide instructions that must be validated first. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | `can_export_presentation` | mitigate | Block failed, unsafe, clarification-only, unvalidated, and empty-result records before spec creation. |
| T-01-08 | Information Disclosure | Template relationships | mitigate | Block `TargetMode="External"` relationships before rendering. |
| T-01-09 | Elevation of Privilege | PPTX macro content | mitigate | Block `vbaProject.bin`, macro-enabled content, and unexpected active content findings. |
| T-01-10 | Denial of Service | Slide spec budgets | mitigate | Enforce text budgets, table limits, and chart limits before rendering. |
| T-01-SC | Tampering | pip package install | mitigate | This plan uses only the dependency approved by Plan 01-01 and added by Plan 01-02. |
</threat_model>

<verification>
Run:
- `$PythonExe = "C:\Users\leonk\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe"; if (Test-Path -LiteralPath $PythonExe) { cmd /c $PythonExe -m unittest evaluation.test_presentation_export } elseif (Get-Command docker-compose -ErrorAction SilentlyContinue) { docker-compose run --rm app /usr/local/bin/python3 -m unittest evaluation.test_presentation_export } else { throw "No bundled Python runtime or docker-compose fallback available." }`
- `$PythonExe = "C:\Users\leonk\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe"; if (Test-Path -LiteralPath $PythonExe) { cmd /c $PythonExe -m compileall src/agent/presentation_export.py evaluation/test_presentation_export.py } elseif (Get-Command docker-compose -ErrorAction SilentlyContinue) { docker-compose run --rm app /usr/local/bin/python3 -m compileall src/agent/presentation_export.py evaluation/test_presentation_export.py } else { throw "No bundled Python runtime or docker-compose fallback available." }`
- Confirm `git diff -- streamlit_app.py` is empty.
</verification>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 1 | Successful validated orchestrator record becomes deterministic Wuerth PPTX bytes, invalid records fail clearly | 01-02, 01-03 | COVERED | Backend-only export path plus fail-closed hardening.
REQ | PPT-02 | Repo-relative tracked Wuerth template | 01-02, 01-03 | COVERED | `DEFAULT_TEMPLATE_PATH` and tests.
REQ | PPT-03 | Block failed, unsafe, clarification-only, missing-result, unvalidated records | 01-03 | COVERED | Eligibility tests and `can_export_presentation`.
REQ | PPT-06 | Missing/unreadable template returns clear error | 01-03 | COVERED | Missing-template tests and structured unavailable export.
REQ | PPT-07 | Backend module, not Streamlit callbacks | 01-02, 01-03 | COVERED | `src/agent/presentation_export.py`; no `streamlit_app.py` edits.
REQ | SPEC-01 | Fixed slide contract with budgets and limits | 01-02, 01-03 | COVERED | Dataclass spec and validation tests.
REQ | SPEC-02 | Reject unsupported shapes before rendering | 01-02, 01-03 | COVERED | `validate_slide_deck_spec`.
REQ | SPEC-03 | Deterministic local rendering, no direct LLM PPTX | 01-01, 01-02, 01-03 | COVERED | Package gate and renderer boundary.
REQ | SPEC-04 | Future optional LLM limited to validated spec JSON | 01-02, 01-03 | COVERED | Public API validates specs and does not accept PPTX/code from models.
REQ | TEST-01 | Template path and missing-template tests | 01-03 | COVERED | Template tests.
REQ | TEST-02 | Generated PPTX non-empty and openable without PowerPoint | 01-02, 01-03 | COVERED | `python-pptx` reopen tests.
REQ | TEST-03 | Eligibility blocks invalid records | 01-03 | COVERED | Table-driven eligibility tests.
RESEARCH | R-01 | `python-pptx` needs legitimacy gate | 01-01 | COVERED | Blocking checkpoint.
RESEARCH | R-02 | Use backend module after reporting output | 01-02 | COVERED | `src/agent/presentation_export.py`.
RESEARCH | R-03 | Warn on known OLE, block macros/external relationships | 01-03 | COVERED | Template safety tests.
RESEARCH | R-04 | Use layout name plus placeholder indexes from manifest | 01-02, 01-03 | COVERED | `TemplateManifest` and validation.
CONTEXT | D-01 | Dynamic ordered repeatable slide list | 01-02 | COVERED | Spec tests.
CONTEXT | D-02 | Optional closing slide | 01-02 | COVERED | Include flag tests.
CONTEXT | D-03 | Template manifest and repo-relative path | 01-02, 01-03 | COVERED | Manifest and path tests.
CONTEXT | D-04 | Think-cell OLE warning, macros/external block | 01-03 | COVERED | Safety tests.
CONTEXT | D-05 | No Streamlit changes in Phase 1 | 01-02, 01-03 | COVERED | Plan actions and verification.
CONTEXT | D-06 | No direct LLM PPTX/code path | 01-01, 01-02, 01-03 | COVERED | API boundary and tests.
CONTEXT | D-07 | Deterministic local renderer with package gate | 01-01, 01-02 | COVERED | Gate and dependency.
</source_audit>

<success_criteria>
- Invalid records and missing templates produce clear unavailable exports with no bytes.
- Template audit treats known think-cell OLE as a warning and blocks macros or external relationships.
- Slide spec validation prevents unsupported renderer inputs before PPTX rendering.
- Successful export behavior from Plan 01-02 remains green.
</success_criteria>

<output>
Create `.planning/phases/01-deterministic-backend-deck-slice/01-03-SUMMARY.md` when done.
</output>
