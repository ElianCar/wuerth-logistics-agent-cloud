---
phase: 01-deterministic-backend-deck-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - SPEC-03
  - TEST-02
must_haves:
  truths:
    - "D-07: The deterministic local PPTX renderer dependency is approved before requirements.txt changes."
    - "D-06: Direct Claude/Opus PPTX generation and runtime LLM-generated slide code are not accepted as production export paths."
  artifacts:
    - path: "requirements.txt"
      provides: "Target dependency file for the approved deterministic PPTX renderer"
  key_links:
    - from: "01-01 dependency approval"
      to: "01-02 requirements.txt"
      via: "Plan dependency"
      pattern: "python-pptx"
---

<objective>
Verify the package legitimacy gate for the deterministic local PPTX renderer before any dependency change.

Purpose: Phase 1 needs local, reproducible `.pptx` generation from a validated slide spec, and research marked `python-pptx` as [ASSUMED] because `pip` and `slopcheck` were unavailable in that shell.
Output: A blocking human approval or rejection that Plan 01-02 must honor before adding `python-pptx` to `requirements.txt`.
</objective>

## Phase Goal

**As a** Wuerth logistics analysis user, **I want to** turn a successful validated orchestrator record into deterministic Wuerth PPTX bytes, **so that** I can get a presentation-ready backend deck output without manual file creation.

<execution_context>
@C:/Users/leonk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/leonk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-deterministic-backend-deck-slice/01-CONTEXT.md
@.planning/phases/01-deterministic-backend-deck-slice/01-RESEARCH.md
@.planning/phases/01-deterministic-backend-deck-slice/01-PATTERNS.md
@.planning/PPT_TEMPLATE_GUIDE.md
@requirements.txt
</context>

<decision_trace>
D-01: Dynamic slide creation must not produce a fixed always-9-slide deck, and Agent 04/05 evidence layouts may repeat.
D-02: Agent 09 Closing is optional and must not be hardcoded into every deck.
D-03: The renderer targets the repo-relative Wuerth template and uses a manifest for layout names, aliases, placeholder indexes, and known warnings.
D-04: Remaining think-cell OLE embeddings are warnings when macros and external relationships are absent; macros and external relationships block export.
D-05: Do not modify `streamlit_app.py` in Phase 1.
D-06: Do not add direct Claude/Opus PPTX generation or runtime LLM-generated slide code.
D-07: Use deterministic local rendering, likely `python-pptx`, behind a package verification gate.
</decision_trace>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify deterministic PPTX renderer package</name>
  <files>requirements.txt</files>
  <read_first>
    - `.planning/phases/01-deterministic-backend-deck-slice/01-RESEARCH.md`
    - `.planning/phases/01-deterministic-backend-deck-slice/01-CONTEXT.md`
    - `requirements.txt`
  </read_first>
  <what-built>No code is built in this gate. It approves or rejects the exact renderer package before Plan 01-02 changes dependencies.</what-built>
  <action>Verify the package named `python-pptx` from PyPI, its linked source repository, release history, and run `slopcheck python-pptx` if available. Approve only `python-pptx` for deterministic local rendering per D-07. Reject adjacent packages that generate decks through cloud services, direct model-produced PPTX files, or runtime LLM-generated slide code per D-06. Record the approval in the plan summary so Plan 01-02 can add exactly this dependency.</action>
  <verify>
    <automated>python -m pip index versions python-pptx</automated>
    <human-check>Confirm `python-pptx` on PyPI is legitimate enough for this repository, or reject it with a reason.</human-check>
  </verify>
  <acceptance_criteria>
    - Human approval names exactly `python-pptx`, or human rejection blocks Plan 01-02 dependency work.
    - The approval notes whether `slopcheck` ran; if it did not, the reason is recorded.
    - No alternative PPTX package or direct LLM PPTX path is approved.
  </acceptance_criteria>
  <done>The package gate result is explicit and Plan 01-02 can decide whether adding `python-pptx` to `requirements.txt` is allowed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI package registry to repository dependency file | A third-party package would become executable code in the project environment. |
| Human approval to automated implementation | Plan 01-02 may add the dependency only if this gate approves it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `requirements.txt` dependency selection | mitigate | Approve only the exact `python-pptx` package after registry/source review. |
| T-01-02 | Repudiation | Package gate approval | mitigate | Require the checkpoint summary to record approved package name and whether `slopcheck` ran. |
| T-01-SC | Tampering | pip package install | mitigate | Blocking human checkpoint for [ASSUMED] package before any install or requirements change. |
</threat_model>

<verification>
This plan is complete when the checkpoint has an explicit approve/reject result for `python-pptx`.
</verification>

<success_criteria>
- `python-pptx` is approved before Plan 01-02 edits `requirements.txt`, or the phase stops with a clear dependency blocker.
- No Streamlit file, router file, SQL agent file, or runtime LLM PPTX path is changed in this plan.
</success_criteria>

<output>
Create `.planning/phases/01-deterministic-backend-deck-slice/01-01-SUMMARY.md` when done.
</output>
